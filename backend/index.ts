// import express from "express";
// import path from "path";

// const app = express();
// const PORT = process.env.PORT || 3500;

// app.post("/onyx/createVC", (req, res) => {});

// app.listen(PORT, () => console.log(`Server running on Port ${PORT}`));
